import React, { Component } from "react";

const Error = () => {
  return (
    <section>
      <p>La página a la que intentas acceder no aparece en la web</p>
    </section>
  );
};

export default Error;
